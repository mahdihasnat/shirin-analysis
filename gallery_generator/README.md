# Gallery Generator

This component generates an HTML gallery to view the frames extracted by the `frame_extractor`.

## Usage

Run the script:
```bash
python3 generate_gallery.py
```

This will create an `index.html` file in this directory. Open it in your browser to view the frames.
